## Packages
framer-motion | For smooth player movement and UI animations
canvas-confetti | For the win celebration effect
@types/canvas-confetti | Types for confetti
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
The game logic (movement, collision) is client-side.
Backend is used for submitting results (win/loss, moves count).
Images/Icons: Using Lucide React for UI elements.
Font strategy: Space Grotesk for headings (tech feel), Inter for UI text.
